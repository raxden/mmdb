<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <activity
            android:name=".ui.CatalogActivity"
            android:exported="true"
            android:icon="@mipmap/ic_catalog_launcher"
            android:roundIcon="@mipmap/ic_catalog_launcher_round"
            android:label="@string/app_name_catalog"
            android:taskAffinity="com.raxdenstudios.app.catalog.ui.CatalogActivity"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
